{
  "name": "social-media-blocks-extension",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-extension": "npm run build && cp manifest.json dist/ && cp -R assets/images dist/assets"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}
