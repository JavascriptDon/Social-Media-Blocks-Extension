{
  "name": "social-media-blocks-extension",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && vite build --config vite.content.config.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "typescript": "^5.0.0",
    "vite": "^4.5.0"
  },
  "dependencies": {
    "animate.css": "^4.1.1",
    "lottie-web": "^5.12.2"
  }
}